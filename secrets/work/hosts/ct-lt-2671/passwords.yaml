# SPDX-FileCopyrightText: 2025 Dom Rodriguez <shymega@shymega.org.uk>
#
# SPDX-License-Identifier: GPL-3.0-only

# Work laptop passwords and basic secrets
# Encrypt this file with: SOPS_AGE_KEY_FILE=~/.config/sops/work/keys.txt sops -e -i passwords.yaml

# User passwords (hashed)
workuser-password: placeholder_hashed_password

# System secrets
luks-password: placeholder_luks_password

# SSH keys
ssh-host-key: |
  placeholder_ssh_host_private_key

# Basic work credentials
work-email-password: placeholder_work_email_password
